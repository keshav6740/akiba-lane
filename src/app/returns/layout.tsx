import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Returns & Exchanges | Akiba Lane",
    description: "Returns and exchange policy for Akiba Lane — learn about our process for damaged or incorrect anime collectible orders.",
};

export default function ReturnsLayout({ children }: { children: React.ReactNode }) {
    return children;
}
